**SECSlider AI — 项目完整梳理**

  

  ---

  **一、整体架构**

  

  ┌─────────────────────────────────────────────────────────┐

  │                     用户浏览器                            │

  │  ┌───────────────────────┐    ┌──────────────────────┐  │

  │  │   前端 React SPA       │    │   Supabase 云端       │  │

  │  │                       │    │                      │  │

  │  │  登录页 (/auth)        │◄───│  Auth 认证服务        │  │

  │  │  主页 (/): 编辑+预览    │    │  PostgreSQL 数据库    │  │

  │  │                       │    │  Edge Function (OCR) │  │

  │  │  导出 PPTX 到本地      │    │  Row Level Security  │  │

  │  └───────────────────────┘    └──────────────────────┘  │

  │         ↑ 部署到 Vercel              ↑ 已在云端运行       │

  └─────────────────────────────────────────────────────────┘

  

  **二、技术栈**

  

  ┌───────────┬──────────────────────────────────────────────────┐

  │    层     │                       技术                       │

  ├───────────┼──────────────────────────────────────────────────┤

  │ 前端框架  │ React 18 + TypeScript                            │

  ├───────────┼──────────────────────────────────────────────────┤

  │ 构建工具  │ Vite 5                                           │

  ├───────────┼──────────────────────────────────────────────────┤

  │ UI        │ Tailwind CSS + shadcn/ui（40+ 组件）             │

  ├───────────┼──────────────────────────────────────────────────┤

  │ 路由      │ React Router v6                                  │

  ├───────────┼──────────────────────────────────────────────────┤

  │ 后端      │ Supabase（PostgreSQL + Auth + Edge Function）    │

  ├───────────┼──────────────────────────────────────────────────┤

  │ AI OCR    │ Google Gemini 2.0 Flash（通过 Lovable API 网关） │

  ├───────────┼──────────────────────────────────────────────────┤

  │ PPTX 导出 │ pptxgenjs                                        │

  ├───────────┼──────────────────────────────────────────────────┤

  │ 包管理    │ Bun                                              │

  └───────────┴──────────────────────────────────────────────────┘

  

  **三、页面结构**

  

  ┌───────┬─────────────────────────┬──────────┐

  │ 路由  │          页面           │ 需要登录 │

  ├───────┼─────────────────────────┼──────────┤

  │ /auth │ 邮件魔法链接登录        │ 否       │

  ├───────┼─────────────────────────┼──────────┤

  │ /     │ 诗歌编辑器 + 幻灯片预览 │ 是       │

  ├───────┼─────────────────────────┼──────────┤

  │ *     │ 404                     │ 否       │

  └───────┴─────────────────────────┴──────────┘

  

  **四、核心功能**

  

  **诗歌录入（3种方式）**

  1. **粘贴文本**：智能解析，自动识别标题、段落、副歌

  2. **OCR 拍照**：拍歌谱 → Gemini 提取歌词 → 自动归段

  3. **从库加载**：已保存的诗歌一键添加到当前编辑

  **诗歌编辑**

  - 编辑标题、段落标签、歌词文本

  - 拖拽调整段落顺序

  - 增删段落

  - AI 分析演唱顺序（主歌→副歌→主歌→结束）

  **幻灯片预览**

  - 实时预览导出效果

  - 缩略图导航、拖拽排序

  - 复制/删除单张幻灯片

  **PPTX 导出**

  - 6套视觉模板（默认/极简/玻璃/活泼/自然/复古未来）

  - 自动字号适配（48-60pt标题 / 40-48pt歌词）

  - 4:3 画幅

  - 装饰元素（棕榈树、教堂logo、"众立同唱"标注）

  - 长歌词自动分页

  **用户系统**

  - 邮箱无密码登录

  - 诗歌云端保存（按用户隔离，RLS 策略保护）

  - 导出时自动保存

  **五、数据流**

  

  用户输入文字/图片

      ↓

  HymnEditor 解析/OCR

      ↓

  useHymns hook（内存状态）

      ↓

  generateSlides() → SlidePreview 展示预览

      ↓

  exportToPptx() → 下载 .pptx 文件（同时自动保存到 Supabase）

  

  **六、数据库**

  

  只有一张表 hymns：

  id              uuid      -- 主键

  user_id         uuid      -- 外键 → auth.users

  title           text      -- 诗歌标题

  verses          jsonb     -- 段落数组 [{id, label, text, isChorus}]

  repeat_structure jsonb    -- 演唱顺序 [0,1,2,1,2]

  created_at      timestamptz

  updated_at      timestamptz

  - RLS 已开启：用户只能看自己的诗歌

  - 去重策略：保存时按"标题 + 第一段前20字"指纹匹配，避免重复

  **七、上线情况**

  

  ┌───────────────────┬─────────────────────────────────────────┐

  │       部分        │                  状态                   │

  ├───────────────────┼─────────────────────────────────────────┤

  │ 数据库 (Supabase) │ **已在云端运行**                            │

  ├───────────────────┼─────────────────────────────────────────┤

  │ 认证服务          │ **已在云端运行**                            │

  ├───────────────────┼─────────────────────────────────────────┤

  │ OCR 接口          │ **已在云端运行**（用的 Lovable Gemini key） │

  ├───────────────────┼─────────────────────────────────────────┤

  │ 前端部署          │ **待部署**（本机 localhost:8080）           │

  └───────────────────┴─────────────────────────────────────────┘

  

  ---

  **简单说**：后端全在云端运行中，只差前端推到 GitHub 并部署到

  Vercel。前端部署完成后，整个系统就全部上线了。