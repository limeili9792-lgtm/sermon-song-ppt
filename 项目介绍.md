# SECSlider AI — 项目架构文档

## 一、产品概述

教会诗歌幻灯片制作工具。支持文字录入/OCR识别歌词、AI分析演唱顺序、生成PPTX幻灯片，导出到本地或保存到云端共享库。

## 二、技术栈

| 层 | 技术 |
|---|---|
| 前端 | React 18 + TypeScript + Vite 5 |
| UI | Tailwind CSS + shadcn/ui |
| 路由 | React Router v6 |
| 构建部署 | Vercel（自动部署） |
| 数据库 | PostgreSQL（Supabase 云端） |
| 认证 | Supabase Auth（邮箱+密码） |
| 存储 | Supabase Storage |
| AI | Google Gemini 2.0 Flash（Edge Function） |
| 代码托管 | GitHub |

## 三、架构图

```
┌─────────────────────────────────────────────────┐
│                   用户浏览器                       │
│                                                  │
│   https://secsliderai.vercel.app                 │
│   React SPA (Vercel 托管)                         │
│                                                  │
│   ┌──────────┐ ┌──────────┐ ┌────────────────┐  │
│   │ 登录注册  │ │ 诗歌编辑  │ │ 预览/导出       │  │
│   │ 邮箱+密码 │ │ 文字+OCR │ │ 本地/云端       │  │
│   └──────────┘ └──────────┘ └────────────────┘  │
└──────┬───────────────┬──────────────────────────┘
       │ REST/Storage   │ HTTP
       ▼                ▼
┌──────────────┐  ┌──────────────┐
│ Supabase A   │  │ Supabase B    │
│ arxwgfif... │  │ cwgxddgos...  │
│              │  │ (Lovable)     │
│ • Auth       │  │               │
│ • hymns 表   │  │ • OCR Edge    │
│ • ppt 表     │  │   Function    │
│ • Storage    │  │ • Gemini API  │
└──────────────┘  └──────────────┘
```

**关键设计：**
- 两个 Supabase 项目分离——数据库/认证（A）和 OCR AI（B）
- 完全绕过 Supabase SDK，全部使用原生 fetch（生产环境兼容性）
- 诗歌库和 PPT 库关闭 RLS，共享访问

## 四、数据库设计

**hymns 表**（诗歌库）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid PK | 主键 |
| user_id | uuid | 创建者 |
| title | text | 诗歌标题 |
| verses | jsonb | `[{label, text, isChorus}]` |
| repeat_structure | jsonb | AI 演唱顺序 `[0,1,2,1]` |
| created_at | timestamptz | 创建时间 |
| updated_at | timestamptz | 更新时间 |

**presentations 表**（云端 PPT）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid PK | 主键 |
| title | text | PPT 名称 |
| note | text | 备注（如"6.7上午"） |
| file_path | text | Storage 路径 |
| creator | text | 创建者邮箱 |
| created_at | timestamptz | 创建时间（10天清理） |

**两表均关 RLS，公开共享；Storage bucket `pptx` 为 Public。**

## 五、认证方案

- 邮箱+密码注册/登录
- Token 存储在 localStorage
- `useAuth` hook 从 localStorage 读取用户状态
- `CallbackHandler` 在 App 入口拦截 hash token 处理登录回调
- 不依赖 Supabase SDK，全部原生 fetch 调用 Auth API

**API 端点：**
- 注册：`POST /auth/v1/signup`
- 登录：`POST /auth/v1/token?grant_type=password`
- 获取用户：`GET /auth/v1/user`（Bearer token）

## 六、数据流

```
用户输入文字/图片
    ↓
HymnEditor（解析/OCR）
    ↓
useHymns hook（内存状态）
    ↓
generateSlides() → SlidePreview 预览
    ↓
exportToPptx()
    ├── returnBlob=false → 下载到本地
    └── returnBlob=true  → 上传到 Supabase Storage → 记录入 presentations 表
```

## 七、核心文件

```
src/
├── App.tsx                    # 路由 + CallbackHandler
├── pages/
│   ├── Auth.tsx               # 登录/注册（直调API）
│   └── Index.tsx              # 主页：编辑器+预览+导出
├── components/
│   ├── HymnEditor.tsx         # 诗歌编辑器
│   └── SlidePreview.tsx       # 幻灯片预览
├── hooks/
│   ├── use-auth.tsx           # 用户认证状态
│   └── use-hymns.ts           # 诗歌CRUD（直调REST API）
├── types/
│   └── slide.ts               # Hymn, SlideData 等类型
├── utils/
│   ├── textParser.ts          # 文字解析
│   ├── pptxExport.ts          # PPTX 生成（pptxgenjs）
│   ├── pptxTemplates.ts       # 6套视觉模板
│   └── cloudStorage.ts        # 云端存储 API
├── assets/                    # 图片资源
└── integrations/supabase/     # Supabase 客户端（已废弃）
```

## 八、部署流水线

```
本地开发 → git push GitHub → Vercel 自动构建部署
                ↑
        Lovable（OCR函数更新）
```

- **前端**：Vercel 托管，`vercel.json` 配置 SPA rewrite
- **数据库/存储**：Supabase 项目 A（arxwgfifkrppkqcqtksr）
- **OCR**：Supabase 项目 B（Lovable，cwgxddgoshyiugggjket）
- **注意**：Lovable 和本地通过 GitHub 同步，合并冲突时 OCR 用 Lovable 版本，其余用本地版本
- **线上地址**：https://secsliderai.vercel.app
